# Required - set a strong, unique password before running the server.
# This value is bcrypt-hashed at startup; the plaintext is never stored or transmitted.
ADMIN_CODE=your_secure_password_here

# Optional - defaults to 3000 for local development
# PORT=3000
